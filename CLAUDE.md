# Tokio Tech Dashboard

## מה זה
גרסת דמו גנרית של דשבורד "מדד בגרות-טק" עירוני, להצגה לרשויות מקומיות.
עיר הדמו: **טוקיו**. הנתונים בגיליון מומצאים. הנתונים הארציים ורשימת
33 הרשויות אמיתיים (משרד החינוך, דו"ח המצוינות 2024), וטוקיו הוצבה ביניהן
כשורת דמו.

קובץ HTML **יחיד** (`index.html`) שמושך נתונים חיים
מגיליון גוגל מפורסם. אין DB, אין קבצי נתונים, אין שלב בנייה.

חי בשתי כתובות, אותו קוד:
- https://tokio-tech-dashboard.vercel.app  (Vercel — מגיש גם את `api/ask`)
- https://ramishaked.github.io/TokioTechDashboard/  (GitHub Pages)

## מה אסור
1. **לא לגעת במערכת של חולון** (ריפו HolonTechDeshboard, פרויקט Vercel
   holon-tech-ask). זה ריפו נפרד לחלוטין.
2. **שום סממן של רשות אמיתית.** לא שם, לא לוגו, לא צבעי מותג. הסיידבר נושא
   סימן מילולי (`.sb-wordmark`) ולא תמונה.
3. **לא לשבור את העיצוב הקיים** בלי אישור: פריסה, sidebar, גדלי פונט,
   מבנה הכרטיסים, סוגי הגרפים.
4. **קובץ אחד באפליקציה.** `index.html` נשאר יחיד. `tools/` הוא אימות
   מקומי בלבד.
5. **חישוב עצמאי.** שורות `סה"כ עיר` בגיליון אינן מקור.
6. `זכאות למדד טק` הוא המדד. לא לסכום פיזיקה+מדמ"ח (יש חפיפה).

## מה שונה מגרסת חולון
| נושא | כאן |
|------|-----|
| שם העיר | `CITY_ME = 'טוקיו'`, ומופיע בטקסטים ישירות |
| פלטה | אינדיגו `#2b4c9b` + אדום `#d9463e`, סיידבר `#0f172a` (`:root`) |
| לוגו | אין. `.sb-wordmark` |
| גיליון | `PUB` מצביע על "נתוני מדד טק גנרי להזנה". 5 טאבים: תשפה (2), תשפו (1), תשפו (2), תשפז (1), תשפז (2). החמישי ריק |
| שורת תוכנית החיזוק | בגיליון נקראת **נינג׳ה טק** (`ROWKEY` מקבל גם `עתידטק`). בממשק: "נינג׳ה טק" |
| מצבת / סה"כ עיר | אין בגיליון. הקוד נופל לסכום בתי הספר ומסמן |
| נתוני העיר ההיסטוריים | `CITY_MOE`, `CITY_HIST`, `CITY_MID`, `CITY_POT_2024`: **מומצאים**, מתועדים ב-`data/national.json` |
| שירות השאלות | `api/ask.js` בשורש הריפו. פרויקט Vercel `tokio-tech-dashboard` מגיש גם את הדף וגם את הפונקציה, Root Directory = שורש. ה-prompt גנרי וקורא את שם העיר מ-`cities.me` |
| localStorage | מפתחות `tokio_*` |

## מבנה הקוד ב-index.html
| אזור | תוכן |
|------|------|
| `<style>` | כל ה-CSS. משתני הצבע ב-`:root` |
| HTML | sidebar, `section.view` לכל תצוגה, חלון "מקור הנתונים" |
| `<script>` | מלכודת שגיאות, helpers, פונקציות `render*`, שכבת הנתונים |

**שכבת הנתונים** בסוף הסקריפט: `PUB` → `GID_MAP` → `discoverTabs` →
`csvParse` → `parseSheet` → `boot()` → `applyPeriod()`. `BUILD` הוא מספר
הגרסה שמוצג בסיידבר: **להעלות אותו בכל שינוי**.

`discoverTabs` מזהה שני פורמטים של דף pubhtml (קישורי `<a>` ישנים, ו-
`items.push({name, gid})` הנוכחי). `GID_MAP` הוא הגיבוי.

להתמצא: `node tools/verify.mjs --map` מדפיס את כל הפונקציות לפי תחום.

## מבנה הנתונים
```js
SCHOOLS = [{ name, note, partial, grades: { 'י': [...15 שדות] } }]
// 0 total  1 cs  2 ph  3 ma5  4 ht  5 cs_g  6 ph_g  7 ht_g
// 8 amat   9 ma5_g  10 ft  11 ft_g  12 ma3  13 ma4  14 en5
FULL = SCHOOLS.filter(s => !s.partial)   // דירוג ונשירה בלבד
```
- מדדים עירוניים → `SCHOOLS`. דירוג ונשירה → `FULL`.
- כל מכנה עירוני עובר דרך `cityDenomByName(שכבה)`.
- חישוב על תקופה אחרת: `withPeriod(key, fn)` בלבד, חישוב טהור בלבד.

## אימות לפני commit
```
npm install --prefix tools      # פעם אחת
node tools/verify.mjs --syntax  # שער תחביר
node tools/verify.mjs           # מלא, עם דפדפן ופיקסצ׳ר
node tools/verify.mjs --live    # מול הגיליון האמיתי
```
`verify.mjs` משווה את הקבועים ב-`index.html` ל-`data/national.json` ונכשל
על כל פער. שינוי ערך במקום אחד מחייב את השני.

## להתאים לעיר הבאה
1. `CITY_ME`, `<title>`, `.sb-title`, ו-`:root` (פלטה).
2. `PUB` לגיליון החדש, `GID_MAP` לטאבים.
3. שורת העיר ב-`CITY_DATA`, `BIG20`, `CITY_NEAR`.
4. `CITY_MOE` / `CITY_HIST` / `CITY_MID` / `CITY_POT_2024` מהמקורות
   הרשמיים, ו-`data/national.json` בהתאם.
5. שם שורת תוכנית החיזוק ב-`ROWKEY` ובטקסטים.
6. פרויקט Vercel חדש ו-`ASK_URL` (או להשאיר את הקיים).
